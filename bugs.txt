termacap:
после перехода по многострочной истории - остаются строки после ввода
перестал работать режим инсерта


parser:
иногда не срабатывает с первого раза
иногда на тех же данных бывает сега

run commands:


out put error: